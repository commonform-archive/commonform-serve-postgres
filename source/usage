Serve Common Forms over HTTP from a PostgreSQL database.

Usage:
  commonform-serve-postgres <database> [options]
  commonform-serve-postgres --help | -h
  commonform-serve-postgres --version | -v

Options:
  -p PORT, --port PORT    HTTP server port [default: 8080]
